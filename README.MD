### A simple rock paper scissors bot using markov chains

This is a simple implementation of markov chains to beat most people at rock paper scissors, especially when following a simple pattern.

The rps.cpp uses one chain with a fixed context length, whereas multibot.cpp uses multiple chains in the same bot with different fixed context lengths.
